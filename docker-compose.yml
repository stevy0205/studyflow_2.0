services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - studyflow_data:/app/data
    command: python3 -m uvicorn api:app --host 0.0.0.0 --port 8000

volumes:
  studyflow_data: